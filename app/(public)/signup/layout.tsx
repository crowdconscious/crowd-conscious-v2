import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: { absolute: 'Regístrate Gratis | Crowd Conscious' },
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
